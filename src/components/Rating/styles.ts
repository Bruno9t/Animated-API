import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    rating:{
        flexDirection:"row",
        marginBottom:10,
        marginTop:5,
        alignItems:"center"
    },
    ratingValue:{
        marginRight:10,
        fontWeight:"700",
        fontSize:15
    }
})

export default styles